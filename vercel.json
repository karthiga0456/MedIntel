{
  "buildCommand": "cd react-dashboard && npm install && npm run build",
  "outputDirectory": "react-dashboard/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
